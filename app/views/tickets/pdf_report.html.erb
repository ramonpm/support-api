<h1><%= Date.today.strftime('%d %b %y') %> - Last one month closed tickets</h1>

<% tickets.each do |ticket| %>
    <hr>
    <dl>
      <dt>Autor:</dt>
      <dd><%= ticket.user.email %></dd>
      <dt>Description:</dt>
      <dd><%= ticket.description %></dd>
      <dt>Closed at:</dt>
      <dd><%= ticket.closed_at.strftime('%d %b %y') %></dd>
    </dl>
<% end %>